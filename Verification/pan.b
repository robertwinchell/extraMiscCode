	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC consumer */
;
		;
		;
		;
		
	case 5: /* STATE 3 */
		;
		now.turn = trpt->bup.oval;
		;
		goto R999;

	case 6: /* STATE 4 */
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC producer */
;
		;
		;
		;
		
	case 9: /* STATE 3 */
		;
		now.turn = trpt->bup.oval;
		;
		goto R999;

	case 10: /* STATE 4 */
		;
		p_restor(II);
		;
		;
		goto R999;
	}

